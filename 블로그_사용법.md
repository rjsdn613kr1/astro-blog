# 블로그 사용법 가이드

> 블로그 주소: https://rjsdn613kr1.github.io/astro-blog/
> 배포 상태: https://github.com/rjsdn613kr1/astro-blog/actions

---

# ⚡ 처음 설정 (딱 한 번만 하면 됨)

새 볼트(astro-blog)에 플러그인을 새로 설치해야 합니다.

## 1단계: Obsidian Git 설치 (자동 업로드)

1. Obsidian 설정(⚙️) → **커뮤니티 플러그인**
2. **제한 모드 끄기** 클릭
3. **탐색** 버튼 → `Obsidian Git` 검색 → 설치 → 활성화
4. 설정 → Obsidian Git → **Auto commit-and-sync interval: `5`** 입력

→ 이제 5분마다 자동으로 GitHub에 업로드됩니다.

---

## 2단계: Templater 설치 (frontmatter 자동 입력)

1. **탐색** 버튼 → `Templater` 검색 → 설치 → 활성화
2. 설정 → **Templater**
3. **Template folder location** 항목에 `templates` 입력
4. **Trigger Templater on new file creation** → **켜기**
5. 아래로 스크롤 → **Folder Templates** → `+` 버튼
   - Folder: `src/data/blog`
   - Template: `templates/새글`

→ 이제 `src/data/blog` 안에 새 파일을 만들면 frontmatter가 자동으로 채워집니다.

---

# 글 쓰는 전체 흐름

```
① Obsidian에서 src/data/blog/ 안에 새 파일 생성
② frontmatter 자동 채워짐 → title, description, category, tags 입력
③ 본문 작성 → Cmd+S 저장
④ 최대 8분 후 블로그 반영
   (빨리 올리려면: Cmd+P → commit-and-sync)
```

---

# 글 쓰는 법

## 파일 만들기

Obsidian 왼쪽 파일 목록에서:
`src` → `data` → `blog` 폴더에 **우클릭 → 새 노트**

**파일명 규칙 (중요!)**
- 영어로 쓰기 (한글은 URL에서 깨질 수 있음)
- 띄어쓰기 대신 `-` 사용

| 좋은 예 | 나쁜 예 |
|---------|---------|
| `tokyo-trip.md` | `도쿄 여행.md` |
| `2026-book-review.md` | `독서감상문.md` |

---

## frontmatter 작성법

파일을 만들면 아래가 **자동으로** 채워집니다 (Templater 설정 후):

```
---
title: tokyo-trip          ← 파일명이 자동으로 들어감
author: suko_yasa
pubDatetime: 2026-04-02T14:30:00+09:00   ← 현재 시각 자동
description: ""
tags: []
draft: false
---
```

**수정해야 하는 항목:**

| 항목 | 설명 | 예시 |
|------|------|------|
| `title` | 블로그에 표시되는 제목 | `도쿄 여행 3박 4일` |
| `description` | 글 목록에 나오는 한 줄 요약 | `처음 가본 도쿄 여행기` |
| `category` | 큰 분류 (하나만) | `에세이` |
| `tags` | 세부 태그 (여러 개 가능) | 아래 참고 |
| `draft` | `false`=공개, `true`=비공개 | |

**완성 예시:**
```
---
title: 도쿄 여행 3박 4일
author: suko_yasa
pubDatetime: 2026-04-02T14:30:00+09:00
description: 처음 가본 도쿄, 먹고 걷고 사진 찍은 기록
category: 에세이
tags:
  - 여행
  - 일본
draft: false
---

여기서부터 본문을 씁니다.
```

---

## tags 쓰는 법

태그가 없을 때:
```
tags: []
```

태그가 하나일 때:
```
tags:
  - 여행
```

태그가 여러 개일 때:
```
tags:
  - 여행
  - 일본
  - 2026
```

---

## 홈페이지 상단에 고정하고 싶은 글

frontmatter에 `featured: true` 추가:
```
---
title: 내 대표 글
featured: true
draft: false
---
```
→ 홈페이지 "Featured" 섹션에 표시됩니다.

---

# 본문 꾸미는 법 (마크다운)

## 제목

```
# 가장 큰 제목
## 중간 제목
### 작은 제목
```

## 글자 강조

```
**굵게**
*기울임*
~~취소선~~
`코드 글씨`
```

결과: **굵게** / *기울임* / ~~취소선~~ / `코드 글씨`

## 목록

```
- 사과
- 바나나
- 딸기
```

```
1. 첫 번째
2. 두 번째
3. 세 번째
```

## 인용구

```
> 이 문장은 회색 박스로 표시됩니다.
```

결과:
> 이 문장은 회색 박스로 표시됩니다.

## 구분선

```
---
```

## 링크

```
[클릭할 텍스트](https://주소)
```

예시:
```
[구글 바로가기](https://google.com)
```

## 표

```
| 이름 | 나이 | 직업 |
|------|------|------|
| 홍길동 | 25 | 개발자 |
| 김영희 | 30 | 디자이너 |
```

## 코드 블록

````
```python
print("안녕하세요")
```
````

---

# 이미지 첨부하는 법

## 방법 1: 드래그 앤 드롭 (추천)

1. Finder에서 사진 파일 찾기
2. Obsidian 글 편집 화면으로 **끌어다 놓기**
3. 자동으로 삽입되고, 파일은 `src/data/blog/images/` 폴더에 저장됨

## 방법 2: 복사 붙여넣기

1. 사진 `Cmd+C` 또는 스크린샷(`Cmd+Shift+4`)
2. 글 편집 화면에서 `Cmd+V`

## 이미지 크기 조절

```
![[사진.jpg|500]]   → 가로 500px
![[사진.jpg|300]]   → 가로 300px
```

---

# 카테고리와 태그

## 카테고리 (큰 분류)

- 글 하나당 하나만
- 홈페이지에 카테고리별로 묶여서 표시됨
- 상단 **Categories** 메뉴에서 확인 가능

```
category: 에세이
```

## 태그 (세부 분류)

- 여러 개 가능
- 상단 **Tags** 메뉴에서 확인 가능

```
tags:
  - 일상
  - 감정
```

## 폴더로 정리하기

`src/data/blog/` 안에 폴더를 만들어도 됩니다:

```
blog/
  에세이/
    첫번째글.md
  스터디/
    파이썬공부.md
  images/       ← 이미지는 여기에 자동 저장
```

---

# 비공개 글

## 임시저장

```
draft: true    → 비공개 (블로그에 안 올라감)
draft: false   → 공개
```

## 영구 비공개 폴더

`src/data/blog/private/` 폴더 안에 넣으면 절대 공개 안 됨.

---

# 글 수정 / 삭제

**수정:** 파일 열고 수정 → `Cmd+S` → 자동 배포

**삭제:** 파일 우클릭 → 삭제 → 자동 배포

---

# 즉시 배포하기

`Cmd+P` → `commit-and-sync` → 엔터

배포 상태 확인: https://github.com/rjsdn613kr1/astro-blog/actions
- ✅ 초록색 = 완료
- 🟡 노란색 = 진행 중 (2~3분 대기)
- ❌ 빨간색 = 오류

---

# 블로그 설정 변경

`src/config.ts` 파일을 **VSCode**로 열어서 수정:

```typescript
title: "suko_yasa",          ← 블로그 제목
author: "suko_yasa",         ← 작성자 이름
desc: "생각과 경험을 기록하는 공간",   ← 블로그 설명
postPerIndex: 4,             ← 홈에 표시할 글 수
```

수정 후 `Cmd+P` → `commit-and-sync`

---

# 소개 페이지 수정

`src/pages/about.md` 파일을 Obsidian에서 열어서 수정.
맨 위 `---` 부분은 건드리지 말 것.

---

# 문제 해결

| 증상 | 해결법 |
|------|--------|
| 새 파일 만들어도 frontmatter 안 나옴 | 위 **처음 설정** 의 Templater 설치 과정 다시 확인 |
| 글이 8분 지나도 안 보임 | `draft: false` 인지 확인 |
| 자동 업로드 안 됨 | Obsidian Git 설치 확인, 또는 `Cmd+P` → `commit-and-sync` 수동 실행 |
| 이미지가 블로그에서 안 보임 | 드래그 앤 드롭으로 다시 첨부 |
| 배포 실패 메일 옴 | actions 페이지에서 오류 확인 |
| 블로그 링크 404 뜸 | 2~3분 기다린 후 새로고침 |
